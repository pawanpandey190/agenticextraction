# API Configuration
DAU_CORS_ORIGINS=["http://localhost", "http://127.0.0.1", "http://your-aws-ip"]

# Redis Configuration (Use service name "redis" in Docker network)
DAU_REDIS_URL=redis://redis:6379/0

# Storage Paths (Matching backend DAU_SESSION_BASE_DIR)
DAU_SESSION_BASE_DIR=/tmp/document_analysis_ui

# LOGS CONFIGURATION
# Logs will be written to the local /logs directory
BACKEND_LOG_PATH=/app/logs/backend-api.log
CELERY_LOG_PATH=/app/logs/celery-worker.log

# API KEYS (Will be loaded from root .env file)
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
