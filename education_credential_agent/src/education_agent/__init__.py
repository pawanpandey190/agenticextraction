"""Education Credential Evaluation Agent.

A production-grade agent to evaluate student education credentials from multiple
document types (PDF, JPEG, JPG, PNG). The agent classifies documents, extracts
qualification data, validates Bachelor's degree semesters, and converts grades
to the French 0-20 scale.
"""

__version__ = "0.1.0"
